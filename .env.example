DB_USER=ttt_stats
DB_PASSWORD=change-me
# Host-side development address; Docker Compose overrides this with the db service hostname.
DB_HOST=localhost
DB_PORT=5432
DB_NAME=ttt_stats

# Host-side development address; Docker Compose overrides this with the redis service hostname.
REDIS_URL=redis://localhost:6379

STATS_INGEST_TOKEN=change-me

STEAM_API_KEY=change-me
STEAM_PROFILE_CACHE_TTL_SECONDS=604800

APP_PORT=3000
